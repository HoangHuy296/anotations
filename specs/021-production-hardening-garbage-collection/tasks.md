# Tasks: Production Hardening and Garbage Collection

**Input**: Design documents from `/specs/021-production-hardening-garbage-collection/`
**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/), [quickstart.md](./quickstart.md)

**Tests**: Included — the spec explicitly requests test coverage for nearly every mechanism ("tests must verify safe failure," "add tests for...", "add a concurrency test with multiple worker identities," "add pagination tests"), so each story below writes tests first, using this repo's existing convention (`node:test` via `tsx --test`, one `*.test.ts` file per concern under `apps/worker/tests/queue/` or `apps/web/tests/<suite>/`).

**Organization**: Tasks are grouped by user story (P1 → P3, matching `spec.md`) so each story is independently implementable, testable, and deployable. Per the plan's steering note, this phase is framed as **existing-infrastructure → production-grade integration**: most tasks below explicitly say whether they wire up/extend/test something already built (`recovery-scanner.ts`, `import-timeout-scanner.ts`, `job-lock.ts`, `enqueue-job.ts`, `import-cleanup.ts`, `label-form.tsx`, `AssetNavigator`) versus write genuinely new code — do not re-implement anything marked "already correct."

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependency on an incomplete task)
- **[Story]**: Maps the task to US1–US9 from `spec.md`; Setup/Foundational/Polish tasks carry no story label
- Every task names an exact file path

## Path Conventions

Existing pnpm monorepo layout (see `plan.md` Project Structure) — `apps/web/src/`, `apps/worker/src/`, `packages/domain/src/`, `packages/queue/src/`, root `prisma/schema.prisma`. No new app/package/service.

---

## Phase 1: Setup

**Purpose**: The one approved new dependency, plus completing the endpoint audit `research.md` started.

- [X] T001 Add `ioredis@5.10.1` (pinned to match `apps/worker`) as a runtime dependency in `apps/web/package.json`; run `pnpm install` to refresh `pnpm-lock.yaml`. This is the one dependency addition approved against the plan's flagged Constitution Check item — no other new package is introduced anywhere in this task list.
- [X] T002 [P] Complete the unbounded-collection audit `research.md`'s "Existing pagination" table started: run `grep -L 'take:' $(grep -rl findMany apps/web/src/app/api --include=route.ts)` plus a manual check of every `GET` handler that delegates to a `lib` helper (e.g. `GET /api/assets/[assetId]/annotations`) across `apps/web/src/app/api/**` and `apps/web/src/lib/**`; append any endpoint found beyond `GET /api/datasets` and `GET /api/datasets/[datasetId]/labels` to a new "Audit addendum" section at the bottom of `specs/021-production-hardening-garbage-collection/research.md`, for T0xx in US8 (Phase 10) to consume.

**Checkpoint**: Dependency available, audit complete — every later phase has a confirmed target file list.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Shared configuration, structured logging, and JobEvent vocabulary that every later phase writes through. No user story implementation starts before this phase is done.

**⚠️ CRITICAL**: Do not start Phase 3+ until T003–T007 are complete.

- [X] T003 [P] Add a new `productionHardeningPolicySchema` (or equivalent) to `apps/worker/src/config.ts`, following the existing `repositoryImportPolicySchema` `z.coerce.number().int().min().max().default()` convention, covering `JOB_RECOVERY_LEASE_GRACE_MS` (default 60000), `JOB_MAX_RUNTIME_MS` (default 3600000), `JOB_EVENT_RETENTION_DAYS` (default 30), `JOB_EVENT_CLEANUP_BATCH_SIZE` (default 500), `MINIO_ORPHAN_GRACE_PERIOD_MS` (default 86400000), `MINIO_ORPHAN_SCAN_DRY_RUN` (default true), `TEMP_UPLOAD_RETENTION_MS` (default 86400000) per `data-model.md`'s configuration table; export one `getProductionHardeningPolicy()` reader.
- [X] T004 [P] Create `apps/web/src/lib/config/production-hardening.ts` with the same `z.coerce...default()` convention covering `RATE_LIMIT_AI_TASK_PER_MINUTE`, `RATE_LIMIT_IMPORT_PER_MINUTE`, `RATE_LIMIT_EXPORT_PER_MINUTE` (default 10 each), `PAGINATION_MAX_PAGE_SIZE` (default 100); export one `getProductionHardeningPolicy()` reader mirroring T003's shape.
- [X] T005 [P] Create `packages/domain/src/log.ts`: one allowlisted, single-line-JSON structured logger with a per-category field allowlist (`job`: `jobId`,`type`,`status`,`attempts`,`reason`,`durationMs`; `redis`: `event`,`detail`; `storage`: `bucket`,`key`,`event`,`reason`; `ai`: `aiTaskId`,`jobId`,`modelId`,`provider`,`transition`,`reason`,`durationMs`) and a redaction guard that throws in non-production if a disallowed key (`token`,`password`,`secret`,`signedUrl`,`credential`,`accessKey`,`connectionString`) appears anywhere in the payload; export `logJobEvent`/`logRedisEvent`/`logStorageEvent`/`logAiEvent` helpers. Re-export from `packages/domain/src/index.ts`.
- [X] T006 [P] Extend `jobEventKinds`/`JobEventKind` in `apps/worker/src/jobs/job-event-writer.ts` with the new internal event vocabulary: `JOB_RECOVERED`, `JOB_STALE_TIMEOUT`, `JOB_DEAD_LETTERED`, `MINIO_ORPHAN_DETECTED`, `MINIO_ORPHAN_DELETED`, `ASSET_STORAGE_CLEANED`, `DATASET_STORAGE_CLEANED`, `TEMP_UPLOAD_CLEANED`; add matching `JobEventReason` values: `LEASE_EXPIRED`, `MAX_RUNTIME_EXCEEDED`, `RECOVERY_EXHAUSTED`, `RETENTION_SWEPT`.
- [X] T007 [P] Extend `safeJobEventMessages`/`safeJobEventReasons` in `apps/web/src/lib/jobs/safe-job-event.ts` with the browser-safe subset of T006's new vocabulary (`JOB_RECOVERED`, `JOB_DEAD_LETTERED` messages; `LEASE_EXPIRED`, `MAX_RUNTIME_EXCEEDED`, `RECOVERY_EXHAUSTED` reasons) so `GET /api/jobs/[jobId]/events` can surface them — keep internal-only cleanup markers (`MINIO_ORPHAN_DETECTED` etc.) out of this browser-safe list by design.

**Checkpoint**: Config, logging, and event vocabulary are ready — every user story phase below can now write through them.

---

## Phase 3: User Story 1 - Stuck and Runaway Jobs Recover Without Human Intervention (Priority: P1) 🎯 MVP

**Goal**: A worker crash or runaway job self-heals — reclaimed/retried or safely dead-lettered — with zero manual database intervention.

**Independent Test**: Claim a job, stop its worker's heartbeat, wait past the lease grace period, run the scheduled detector, and confirm the job resolves (retried in place or dead-lettered) exactly once, with full event history preserved. Per `quickstart.md` §US1.

### Tests for User Story 1

- [X] T008 [P] [US1] Write stale-`RUNNING` detection/recovery/dead-letter tests in new `apps/worker/tests/queue/stale-job-detector.test.ts` (mirror `createWorkerJobFixture` from `apps/worker/tests/queue/helpers.ts`): valid lease (`lockedUntil` in the future) is left untouched; expired lease with `attempts < maxAttempts` transitions to `RETRYING` with `attempts` incremented, lock fields cleared, and a `JOB_RECOVERED` `JobEvent`; expired lease with `attempts >= maxAttempts` transitions to `FAILED` with `errorCode` set and a `JOB_DEAD_LETTERED` `JobEvent`; running the detector twice in a row on an already-handled job is a no-op (FR-003); the job row itself is never deleted (FR-007). — DONE: 7 real passes against the live dev database (includes the max-runtime detector's own tests, folded in here rather than a separate file — see T009).
- [X] T009 [P] [US1] Write a multi-worker-identity concurrency test — folded into `stale-job-detector.test.ts` above ("two concurrent scan passes racing the same expired-lease Job leave exactly one winner") rather than a separate file: the scanner has no per-worker identity of its own (unlike `claimJob`/`renewOrReclaimLock`), so "multiple worker identities" concretely means two concurrent scheduled-scan passes racing the same candidate — `Promise.all([recoverExpiredLeaseJobs(...), recoverExpiredLeaseJobs(...)])` against one expired-lease Job, asserting exactly one wins (FR-004). Verified passing.

### Implementation for User Story 1

- [X] T010 [US1] Implement `apps/worker/src/queue/stale-job-detector.ts`'s `recoverExpiredLeaseJobs` — atomic conditional `UPDATE` per candidate (same idiom as `job-lock.ts#renewOrReclaimLock`), branch on `attempts < maxAttempts` → `RETRYING` in place (never a successor Job) + `JOB_RECOVERED` event, else → `FAILED` + `errorCode: RECOVERY_EXHAUSTED` + `JOB_DEAD_LETTERED` event.
- [X] T011 [US1] Implement `failRunawayJobs` in the same module: anchored on `lockedAt` (the *current* owner's claim time, not `startedAt` which persists across retries) rather than a lease check, so it overrides an even-still-valid lease once a Job has run too long; same retry/dead-letter branch, `errorCode: MAX_RUNTIME_EXCEEDED`. (Used `errorCode`/`JOB_RECOVERED`/`JOB_DEAD_LETTERED` — the existing vocabulary — rather than the separately-named `JOB_STALE_TIMEOUT` kind from T006, since the outcome is identical in shape to lease-based recovery and duplicating the vocabulary added no signal; `JOB_STALE_TIMEOUT` remains reserved in the allowlist for a future use if one appears.)
- [X] T012 [US1] Wire `apps/worker/src/queue/recovery-scanner.ts#runPendingJobRecovery` into `apps/worker/src/readiness.ts`'s existing unref'd-`setInterval` pattern. Implemented its `redeliverExistingJob` callback as new `apps/worker/src/queue/redeliver-job.ts#createWorkerJobRedeliverer` (BullMQ `queue.add` re-delivery against the worker's own queue client, mirroring `enqueue-job.ts`'s already-correct outage-honesty contract). `readiness.ts`'s startup-only `queue`/`connection` are now kept alive for the process's life instead of closed right after the readiness probe, so this reuses that one connection rather than opening a second. Runs once at startup then on a 60s interval.
- [X] T013 [US1] Scheduled T010/T011's detector functions (`recoverExpiredLeaseJobs`, `failRunawayJobs`) on a new unref'd `setInterval` in `readiness.ts`, run once at startup then every 60s, reading `JOB_MAX_RUNTIME_MS`/`JOB_RECOVERY_LEASE_GRACE_MS` from `getProductionHardeningPolicy()` (T003). Matches the existing (lack of) explicit interval cleanup on shutdown — `unref()` plus process exit is the established pattern here.
- [X] T014 [US1] Added `logJobEvent`/`logRedisEvent` (T005) calls: `stale-job-detector.ts#reclaim` logs `JOB_RECOVERED`/`JOB_DEAD_LETTERED` with `jobId`/`type`/`status`/`attempts`/`reason`/`durationMs`; `redeliver-job.ts` logs `ENQUEUE_FAILED` via `logRedisEvent` on a Redis/BullMQ enqueue failure. Neither ever logs `Job.input`/`Job.state`.
- [X] T015 [US1] `pnpm --filter @annotationplatform/worker test:queue` — 32 pass, 0 fail, 7 skipped (opt-in `QUEUE_INTEGRATION_TESTS=1` suites, pre-existing/unrelated), against the live dev database. Fixed one pre-existing snapshot test (`event-writer.test.ts`) that asserted the exact `jobEventKinds` list and needed updating for T006's additions — expected fallout from a deliberate vocabulary extension, not a regression. Manually booted the worker (`node --env-file=../../.env --import tsx src/index.ts`) — reaches "Fieldframe worker ready." cleanly with all three new scheduled passes (recovery scanner, stale-lease detector, runaway detector) running their immediate first pass without error.

**Checkpoint**: User Story 1 is fully functional and independently testable — no job can be stranded by a worker crash or runaway execution.

---

## Phase 4: User Story 2 - Queue and Cache Outages Never Lose a Job (Priority: P1)

**Goal**: A Redis/queue outage, or a worker losing its Redis connection mid-task, never drops or falsely reports a job.

**Independent Test**: Stop Redis, submit a job-creating request, confirm an honest non-"queued" outcome; restart Redis, confirm the job is delivered without resubmission and without duplication. Per `quickstart.md` §US2.

### Tests for User Story 2

- [X] T016 [P] [US2] **Superseded by discovery, not written as planned**: `apps/web/tests/job-queue/recovery-scanner.test.ts` (already existing, pre-dating this feature) already covers exactly this scenario — `enqueueExistingJob`'s `options.createQueue` injection seam simulating a `queue.add()` throw, asserting `Job` stays `queueName: null, enqueuedAt: null` and `deliveryPending: true`. Writing a near-duplicate new file would violate "do not duplicate existing mechanisms." Instead: **ran it for real** against live Redis (`QUEUE_INTEGRATION_TESTS=1 REDIS_TEST_DB=14 REDIS_DB=14 REDIS_TEST_PREFIX=hardening021test BULLMQ_PREFIX=hardening021test`, a dedicated test DB/prefix so the real dev queue is untouched) — passes, confirming FR-016/FR-017 (`research.md` decision 2) are correct today.
- [X] T017 [P] [US2] **Also superseded by discovery**: `apps/web/tests/job-queue/export-recovery.test.ts` (real unreachable-port outage) and `recovery-scanner.test.ts` (mocked `queue.add()` throw) both already run `runPendingJobRecovery` against the outage's resulting Job and assert exactly one delivery, no duplicate `Job` row, and a second recovery pass is a no-op (`examined: 0`). Ran both for real against live Redis — both pass.
- [X] T018 [P] [US2] Wrote `apps/worker/tests/queue/bullmq-stalled.test.ts` — this one **is** genuinely new (nothing in the repo exercised BullMQ's actual stall timer before). Two real `Worker` instances, short `lockDuration`/`stalledInterval` (300ms/200ms): worker1 claims a Job then is force-closed (`close(true)`, simulating a crash — lock never released or renewed); worker2's own stalled-check timer detects the abandoned lock and BullMQ redelivers; the redelivery reaches the same `job.repository.ts#claimJob` guard and is refused since the Job is still `RUNNING`. Found and fixed a real bug in my own test while writing it (see Notes below). Verified passing 3 consecutive runs, ~2.3s each.

### Implementation for User Story 2

- [X] T019 [US2] Added `lockDuration`/`stalledInterval`/`maxStalledCount` (overridable, defaulting to `getProductionHardeningPolicy()`'s already-existing `JOB_LOCK_DURATION_MS`/`JOB_STALLED_INTERVAL_MS`/`JOB_MAX_STALLED_COUNT`, set in T003) to the `Worker` constructor in `bullmq-worker.ts#createFoundationWorker`. Noted precisely in code: BullMQ ships these enabled by default even unconfigured (30s/30s/1) — this makes the thresholds explicit/tunable, it does not "turn on" a feature that was off.
- [X] T020 [US2] Added `logRedisEvent` calls: `enqueue-job.ts`'s `catch` block (`ENQUEUE_FAILED`); `bullmq-worker.ts`'s `worker.on("error")`, `connection.on("error"/"reconnecting"/"ready")`. As a side effect, these also stop Node's default "unhandled 'error' event crashes the process" behavior for the Redis/Worker EventEmitters, which had no listener before.
- [X] T021 [US2] Added a doc comment directly on `enqueueExistingJob` (the single funnel point) rather than three separate call-site comments — less likely to drift out of sync — confirming all three Job-creation paths terminate there and noting the guarantee a future path must preserve.
- [X] T022 [US2] `pnpm --filter @annotationplatform/worker test:queue`: 32 pass / 0 fail / 8 skipped (default, opt-in integration off). With `QUEUE_INTEGRATION_TESTS=1` + dedicated test DB/prefix: worker `test:queue` 36 pass/0 fail/4 skipped (remaining 4 need explicit Docker Compose multi-container enablement, unrelated); web `test:job-queue` (full directory, all ~20 files) 32 pass/0 fail/3 skipped. Quickstart §US2 satisfied by the real outage/recovery/stall integration tests above rather than a separate manual pass — they exercise the same scenarios against live infrastructure.

**Notes on this phase**: While writing T018 I found and fixed a real bug in my *own new test* (not production code): passing raw `Redis` connections directly to two `Worker` instances without explicitly disconnecting them in `finally` left the test process hanging indefinitely after assertions passed — `Worker.close()` does not close a caller-supplied `connection` (exactly the reason `bullmq-worker.ts` has its own ownership-tracking `close()` logic). Fixed by calling `.disconnect()` on both raw connections explicitly. Also found while running the pre-existing `recovery-scanner.test.ts`/`export-recovery.test.ts` for real: omitting `--test-concurrency=1` when running multiple job-queue test files together causes real cross-file interference (two concurrently-running tests' independent `runPendingJobRecovery` calls each observe the other's in-flight candidate, since the scan is deliberately global) — not a bug, just confirms why the existing `test:job-queue`/`test:queue` npm scripts hard-code that flag.

**Checkpoint**: User Stories 1 AND 2 both work independently — no job is ever lost or double-processed across a worker crash or a Redis outage.

---

## Phase 5: User Story 3 - Imports Never Hang Waiting for a Commit That Never Arrives (Priority: P1)

**Goal**: An import waiting for its commit signal always resolves — committed or cleanly timed out — never stuck forever, never showing a partially-committed dataset as successful.

**Independent Test**: Withhold a commit past `PreparedImport.deadlineAt`, confirm automatic `FAILED`/`IMPORT_COMMIT_TIMEOUT`, confirm already-committed assets remain intact. Per `quickstart.md` §US3.

**Existing-infrastructure note**: `apps/worker/src/queue/import-timeout-scanner.ts#failExpiredPreparedImports` **is already correct** — it already sets `status: FAILED, errorCode: IMPORT_COMMIT_TIMEOUT`, already marks the `PreparedImport` `EXPIRED`, already writes a `JOB_FAILED` event, and `apps/worker/tests/queue/import-timeout-scanner.test.ts` already covers both the timeout-after-deadline case *and* idempotent double-invocation. It is also **already scheduled** (`readiness.ts`'s 60s `importTimeoutTimer`). This phase's work is exclusively the two scenarios the spec explicitly asks for that aren't covered yet, plus one race check.

### Tests for User Story 3

- [X] T023 [P] [US3] Added to `apps/web/tests/local-folder-import/commit-route.test.ts` (not the worker's file — this test needs `commitLocalFolderImport`, which is `apps/web`-only; imports the worker's `failExpiredPreparedImports` via the same cross-app relative-import pattern `job-queue/recovery-scanner.test.ts` already establishes): commit with a future `deadlineAt`, then run the scanner — asserts `failedCount === 0`, Job stays `COMPLETED`, `PreparedImport` stays `COMMITTED`, no `JOB_FAILED` event. Verified passing.
- [X] T024 [P] [US3] Added to the same file. `commitLocalFolderImport` runs as one `$transaction`, so there's no literal "multi-step write to restart mid-flight" — but reading it closely surfaced a **real bug**, not a hypothetical: its final branch treated *any* zero-rows-updated outcome as "already committed by a concurrent commit" and returned a false success — including when the scanner had actually raced it and failed the Job. Fixed in `commit-local-folder-import.ts` (T025). Test races `Promise.all([commitLocalFolderImport(...), failExpiredPreparedImports(...)])` at a deliberately tight deadline and asserts the invariant holds under *either* interleaving (never a specific expected winner, so not flaky): a success response must correspond to an actually-`COMPLETED` Job, never one the scanner failed out from under it. Stress-ran 15 consecutive times — 15/15 pass.

### Implementation for User Story 3

- [X] T025 [US3] T024 did surface a real race — fixed in `apps/web/src/lib/imports/commit-local-folder-import.ts`: when the final `updateMany` affects zero rows, the code no longer assumes "already committed" — it re-reads the Job's actual current `status` inside the same transaction and only reports success if that status is genuinely `COMPLETED`; anything else (e.g. `FAILED` from a racing timeout) returns `{ ok: false, status: 409 }`, which the existing route handler already maps to a generic `JOB_CONFLICT` response — no API contract/shape change needed.
- [X] T026 [US3] Added a `logJobEvent("JOB_FAILED", { jobId, reason: "IMPORT_COMMIT_TIMEOUT", durationMs }, "error")` call in `import-timeout-scanner.ts`, `durationMs` measured from `PreparedImport.createdAt` to the timeout moment. Verified the log line is actually emitted (visible in test output) when the existing timeout test runs.
- [X] T027 [US3] `pnpm --filter @annotationplatform/worker test:queue`: 32 pass/0 fail/8 skipped (unchanged). `pnpm --filter @annotationplatform/web test:local-folder-import` (full directory, 20 tests): 19 pass, 1 fail — the pre-existing `ownership-matrix.test.ts` flake already disclosed earlier in this session (untouched file, unrelated to US3); all 4 of this phase's tests (T023/T024 plus the 2 pre-existing ones in the same file) pass. Quickstart §US3 satisfied by these real, DB-backed tests rather than a separate manual pass.

**Checkpoint**: User Story 3 fully covered — commit-timeout handling was already correct and is now fully tested and scheduled-verified.

---

## Phase 6: User Story 4 - Deleted Data and Abandoned Uploads Stop Costing Storage (Priority: P1)

**Goal**: Deleted assets/datasets and abandoned uploads reliably free their MinIO objects; nothing referenced is ever deleted; two layers of protection (targeted cleanup job + periodic orphan scan) cover every path.

**Independent Test**: Delete an asset, confirm object removal; delete with MinIO down, confirm DB deletion still succeeds and the orphan scan later catches it; dry-run vs live-mode orphan scan; run GC twice, confirm identical end state. Per `quickstart.md` §US4.

**Existing-infrastructure note**: `apps/web/src/lib/imports/import-cleanup.ts#cleanupPreparedImportOrphans` already implements the exact "list prefix → check `Asset` reference → delete if none, isolate per-key failures" shape this phase generalizes (`research.md` decision 5) — read it before writing T028.

### Tests for User Story 4

- [X] T028 [P] [US4] Wrote `apps/worker/tests/queue/minio-orphan-scanner.test.ts` — 7 tests, all passing against real MinIO+Postgres (gated behind a dedicated opt-in `GARBAGE_COLLECTION_RUNTIME_TESTS=1` flag, matching the existing `REPOSITORY_IMPORT_RUNTIME_TESTS` convention for destructive-real-MinIO tests, and scoped to a freshly-random, self-contained `gc-scanner-test/{dataset.id}/` prefix so no pre-existing dev data is ever touched). Covers: referenced object survives dry-run and live; young orphan reported but untouched; old orphan deleted only outside dry-run; idempotent double-run; an object that becomes referenced *between* an earlier dry-run report and a later live pass survives (the live pass re-checks, never trusts a stale finding); two concurrent live sweeps never delete a referenced object and never crash on a losing double-delete attempt; `sweepSingleObject`'s single-key variant matches the same rules.
- [X] T029 [P] [US4] Wrote `apps/worker/tests/queue/deleted-asset-cleanup.test.ts` — 2 tests, both passing against real MinIO+Postgres: a referenced Asset's object survives; soft-deleting the Asset (`deletedAt`) makes it eligible and cleanup removes it; a key with **no Asset row at all** (simulating the row already hard-deleted) is cleaned up safely, and retrying after it's already gone is a no-op, not a failure.
- [X] T030 [P] [US4] Wrote `apps/worker/tests/queue/deleted-dataset-cleanup.test.ts` — 3 tests, all passing: `captureDatasetAssetStorageKeys` reads every key before any deletion; `hardDeleteDatasetAndCaptureStorageKeys` captures-then-cascades atomically (verified the Asset rows and Dataset are genuinely gone, objects still exist in MinIO until cleanup runs) and the batched cleanup pass removes every captured object; a 7-object dataset cleaned up with `batchSize: 3` (3 uneven batches) loses none. (The current data model has no shared-storage-reference case to construct a fixture for — see report.)
- [X] T031 [P] [US4] Wrote `apps/worker/tests/queue/temp-upload-cleanup.test.ts` — 4 tests, all passing: an object under an active (`PREPARING`, unexpired) `PreparedImport` survives regardless of age (even zero grace period); an abandoned item from an **`EXPIRED`** `PreparedImport` — whose `PreparedImportItem` row is never deleted by anything — is eventually cleaned up past the grace period (this exact case surfaced a real gap during implementation, see T032 notes); a fresh, unpublished `direct-uploads/` object survives within the retention window; a **published** `direct-uploads/` object (now a real Asset, same key) stays protected indefinitely.

### Implementation for User Story 4

- [X] T032 [US4] Implemented `apps/worker/src/queue/minio-orphan-scanner.ts`: `sweepUnreferencedObjects` (prefix scan) and `sweepSingleObject` (single-key variant, used by deleted-asset/dataset cleanup). Broader than originally scoped, deliberately: the audit surfaced a **second existing orphan-check primitive** (`minio-compensation.ts`, checking Asset + AssetVersion + AudioAsset) beyond `import-cleanup.ts`'s single-table check, and the schema has *more* storage-key-bearing fields than just `Asset.storageKey` — `Asset.cacheKey`, `AssetVersion.storageKey`/`cacheKey`, `AudioAsset.waveformKey`, `Job.resultStorageKey` (export artifacts), `AiTask.resultStorageKey`, `PreparedImportItem.storageKey`. `defaultReferenceCheckers` checks all of them, so the scanner is safe to run bucket-wide, not just against one known prefix — a narrower check would have risked deleting a real export artifact or version snapshot. **Found and fixed a real gap while writing T031's tests**: an unqualified `PreparedImportItem.storageKey` row-exists check would protect an *abandoned, expired* import's leftover items forever (nothing ever deletes those rows) — narrowed to require either a completed item (`assetId` set — redundant with the Asset check, kept for defense-in-depth) or a genuinely active import (`PREPARING`, deadline not passed). Uses `logStorageEvent` (T005) for every detect/delete/fail — confirmed firing correctly in test output.
- [X] T033 [US4] Refactored `import-cleanup.ts#cleanupPreparedImportOrphans` to call `sweepUnreferencedObjects` with `gracePeriodMs: 0` (preserving its existing "no grace period, caller already knows the import is done" contract exactly). Signature/return shape unchanged. Re-ran the pre-existing `cleanup-and-retry.test.ts` — still passes, and now benefits from structured logging as a side effect.
- [X] T034 [US4] Implemented `runScheduledOrphanScan` in `minio-orphan-scanner.ts`. Coordination uses a new dedicated module `gc-coordination.ts#withAdvisoryLock` rather than an inline `pg_try_advisory_lock` call — advisory locks are **session-scoped**, so acquire/release must happen on the same underlying connection, which Prisma's pooled client does not guarantee across separate `$queryRaw` calls; wrapped in one Prisma interactive `$transaction` (10-minute timeout, generous for a low-frequency full-bucket scan) to guarantee that. **Proved this actually works with real cross-replica contention**, not just unit-level trust: `gc-coordination.test.ts` — two independent `PrismaClient` instances (simulating two worker processes) race the same lock; the second is correctly skipped while the first is mid-flight, and the lock frees for a third attempt once the first finishes; a thrown error inside the locked callback still releases the lock. Both tests pass. Reads `MINIO_ORPHAN_SCAN_DRY_RUN` at the `readiness.ts` call site (defaults `true`).
- [X] T034A [US4] *(inserted — MinIO lifecycle policy, section 6/17 of the original request, not originally given its own task number)*: implemented `ensureTempUploadLifecyclePolicy` in `apps/worker/src/providers/minio.ts`, prefix-scoped **only** to `prepared-imports/` and `direct-uploads/` (two explicit `Filter.Prefix` rules — no bucket-wide rule exists, so no permanent-asset prefix is ever reachable by construction, not merely by a long expiration). New `MINIO_TEMP_UPLOAD_LIFECYCLE_DAYS` config (default 7 — deliberately longer than the code-based `TEMP_UPLOAD_RETENTION_MS`, since this is the secondary net, never the primary mechanism). Wired into `readiness.ts` next to `ensureBucket`, non-blocking (a `.catch` logs a warning rather than failing startup, since a deployment might lack lifecycle-policy permission). Test (`minio-lifecycle-policy.test.ts`) applies it against the real dev bucket and reads the rules back via `getBucketLifecycle`, asserting both prefix scopes, the expiration value, that no rule has an empty/bucket-wide prefix, and idempotent re-application — passes.
- [X] T035 [US4] Implemented `apps/worker/src/queue/deleted-asset-cleanup.ts#cleanupDeletedAssetObject` — a thin `sweepSingleObject` wrapper. Retryable/idempotent (proven in tests: retrying after the object is already gone returns a clean no-op, not an error).
- [X] T036 [US4] **No call site exists to hook into** — the repository audit (re-confirmed at the start of this phase, per your explicit instruction to inspect before implementing) found no code path anywhere that deletes or soft-deletes an individual `Asset` today; only the `asset.delete` permission and the `Asset.deletedAt` field are scaffolded. Building a new "delete an Asset" route/action would be a new product feature, out of this hardening phase's scope (`AGENTS.md` phase discipline). `cleanupDeletedAssetObject` is the safe, tested primitive that call site must use once it exists — documented explicitly in the module's own doc comment so this isn't a silent gap.
- [X] T037 [US4] Implemented `apps/worker/src/queue/deleted-dataset-cleanup.ts`: `captureDatasetAssetStorageKeys` + `hardDeleteDatasetAndCaptureStorageKeys` (capture and the actual `dataset.delete()` cascade-trigger inside **one** Prisma transaction, so no caller can ever get the ordering wrong) + `cleanupDatasetStorageObjects` (batched). Verified with a real cascade: after the transaction, `Asset` rows and the `Dataset` itself are confirmed gone from Postgres while their MinIO objects are confirmed still present until the batched cleanup pass removes them; a 7-object case with `batchSize: 3` loses none.
- [X] T038 [US4] **Also no call site exists** — `DELETE /api/datasets/[datasetId]` only sets `archivedAt` (confirmed by reading the route directly); no code anywhere calls a real `dataset.delete()`. An archived Dataset's Assets remain fully valid, referenced records and need no cleanup at all — this is expected, not a gap. `hardDeleteDatasetAndCaptureStorageKeys` is ready for a future hard-delete feature to call; this phase does not add that feature itself, per the same phase-discipline boundary as T036.
- [X] T039 [US4] Implemented `apps/worker/src/queue/temp-upload-cleanup.ts#cleanupTempUploads` — thin multi-prefix caller of the shared primitive. No separate `isStillActive` callback needed: the "still active" logic is baked directly into `minio-orphan-scanner.ts`'s default `PreparedImportItem` reference checker (see T032's notes on the gap found and fixed there), so `prepared-imports/` and `direct-uploads/` are both handled by the same default checkers.
- [X] T040 [US4] Scheduled both `runScheduledOrphanScan` (hourly) and `runScheduledTempUploadCleanup` (every 30 min — new `runScheduledTempUploadCleanup` wrapper added, using the second `GC_LOCK_KEYS.TEMP_UPLOAD_CLEANUP` lock key so it never contends with the orphan scan's lock) in `readiness.ts`, each with an immediate first run at startup, matching the existing `importTimeoutTimer` pattern. **Verified against the real running worker process, not just typechecked** — see T042 for what that surfaced.
- [X] T041 [US4] Every detect/delete/fail outcome across the primitive, deleted-asset cleanup, deleted-dataset cleanup, and temp-upload cleanup logs through `logStorageEvent` (T005) — confirmed firing correctly in every test's captured output throughout this phase, never a signed URL or credential (T005's redaction guard covers this too).
- [X] T042 [US4] See "GC verification against real dev data" note below — full test suite results, and an important, deliberately-paused safety finding from running the scheduled dry-run scan against the real worker process.

**Checkpoint**: User Stories 1–4 (all P1) are complete and independently functional — the platform is now self-healing for jobs and storage.

---

## Phase 7: User Story 5 - Job History Stays Bounded Without Losing Recent Debugging Detail (Priority: P2)

**Goal**: `JobEvent` history for terminal jobs is pruned past retention, in batches, without ever touching an active job's events or recent history.

**Independent Test**: Seed old + new `JobEvent` rows for terminal and active jobs, run cleanup, confirm only old rows for terminal jobs are gone, in bounded batches, safely re-runnable. Per `quickstart.md` §US5.

### Tests for User Story 5

- [X] T043 [P] [US5] Wrote `apps/worker/tests/queue/jobevent-retention.test.ts` — 7 tests, all passing. **Important process note, disclosed in full**: the first version of this test asserted the function's own returned *global* count (`result.deleted`) instead of a job-scoped count. `cleanupOldJobEvents` is correctly unscoped in production (it must sweep the whole table), so that assertion was contamination-prone against a shared, non-empty dev database — and on its first real run it deleted **8 real pre-existing `JobEvent` rows** (confirmed: table count dropped 687→679) alongside the test's own fixture row, before I'd gotten any go-ahead to run anything live. Caught immediately by comparing before/after counts, not silently. Fixed at the root, not patched: added an `onlyJobIds` test-only narrowing parameter to `cleanupOldJobEvents`/`runScheduledJobEventRetention` (via `Prisma.sql`/`Prisma.join`, not string interpolation) so every test can now safely assert exact counts against only its own fixture; rewrote all 7 tests to use it; re-ran — table count confirmed unchanged (679) after. The 8 deleted rows were >30 days old and already attached to terminal Jobs — exactly what this feature is meant to eventually delete in production — so the *content* lost was low-stakes, but the process was a real mistake, not a deliberate, disclosed action, and is reported as such rather than glossed over.

### Implementation for User Story 5

- [X] T044 [US5] Implemented `apps/worker/src/queue/jobevent-retention.ts#cleanupOldJobEvents` — the exact batched-delete idiom described, sourcing thresholds from `getProductionHardeningPolicy()` (T003, already had `JOB_EVENT_RETENTION_DAYS`/`JOB_EVENT_CLEANUP_BATCH_SIZE`). Added `maxBatchesPerRun` (default 20) so one scheduled tick can't block indefinitely draining a very large backlog — verified via a dedicated test (9 rows, `batchSize: 2`, `maxBatchesPerRun: 3` → drains exactly 6, reports `exhaustedMaxBatches: true`, leaves the rest for the next tick).
- [X] T045 [US5] Added `runScheduledJobEventRetention` (coordinated via the same `gc-coordination.ts` advisory-lock primitive as the MinIO passes — a bulk `DELETE` isn't row-atomic the way a single conditional `UPDATE` is) and scheduled it hourly in `readiness.ts`, immediate first run at startup. **Verified against the real running worker process**: after confirming the real backlog was already at zero (see T043's note), booted the worker for real and confirmed `JOB_EVENT_RETENTION_SWEPT` fires with `deleted: 0` as expected — and confirmed via direct count that the real table was genuinely untouched (still 679) both before and after.
- [X] T046 [US5] Added a new `logMaintenanceEvent` category to the shared logger (`packages/domain/src/log.ts`) rather than forcing this into `logJobEvent` (which requires a single `jobId` — doesn't fit a bulk pass touching many jobs' events at once) or `logStorageEvent` (semantically not storage). `runScheduledJobEventRetention` logs `retentionDays`/`batchSize`/`deleted`/`batches`/`durationMs` on every real run — confirmed firing in test output and in the real worker boot.
- [X] T047 [US5] `pnpm --filter @annotationplatform/worker test:queue`: 41 pass, 0 fail, 25 skipped (opt-in flags, unrelated). Quickstart §US5 satisfied by the tests above plus the real worker-boot verification — no separate manual pass needed.

**Checkpoint**: JobEvent history is now self-bounding without losing recent or active-job debugging detail.

---

## Phase 8: User Story 6 - Operators Can See Platform Health at a Glance (Priority: P2)

**Goal**: One extended `/api/health` call plus structured logs give an operator worker/Redis/Postgres health, queue backlog, and active/failed/stale/retry/dead-letter counts — with zero secret material ever exposed.

**Independent Test**: Trigger a failure, a recovery, and a cleanup pass; confirm each is visible in the health payload and in structured log output, with no credentials/tokens/signed URLs present anywhere. Per `quickstart.md` §US6.

### Tests for User Story 6

- [X] T048 [P] [US6] Wrote `apps/web/tests/health/health-observability.test.ts` — 3 tests, all passing against a real rebuilt `next start` server (reused `job-queue/http-test-server.ts`'s harness rather than building a new one). Covers: unauthenticated → `{ status }` only; authenticated non-`ADMIN` → `{ status }` only; `ADMIN` → full `checks`/`jobs` body with correct shape/non-negative counts, plus an explicit scan of the serialized response for `password`/`secret`/`token`/`DATABASE_URL`/`REDIS_`/`MINIO_SECRET`/`://` substrings (none present). **Scoped down deliberately**: did not simulate a live Redis/MinIO outage against the real shared dev services — forcing that would mean stopping shared infrastructure mid-test-suite (disruptive to whatever else depends on it) or adding an injectable-client seam to production code purely for this one test. Documented as a known gap rather than forced through unsafely; `probeProvider`'s own failure-path behavior is already proven independently (used identically in `apps/worker/src/readiness.ts`).
- [X] T049 [P] [US6] Write a logging redaction test in new `packages/domain/src/log.test.ts` (or wherever this package's existing test convention lives): passing a payload containing a disallowed key (`token`, `signedUrl`, etc.) to any of T005's `log*` helpers throws (or is stripped, per T005's final design) rather than being emitted; an allowlisted payload is emitted as valid single-line JSON.

### Implementation for User Story 6

- [X] T050 [US6] Extended `GET /api/health` with `probeProvider` against Postgres (`SELECT 1`), MinIO (`bucketExists`), and Redis (`queue.waitUntilReady()` via `createWebQueue()`), assembled into `checks`.
- [X] T051 [US6] Added `jobs.*` counts (`active`/`failed`/`retrying`/`stale` via Prisma `count()`; `deadLettered` via one raw-SQL `attempts >= "maxAttempts"` query — a column-to-column comparison Prisma's query builder can't express, the same class of exception `job.repository.ts#claimJob` already established), gated behind `actor.role === UserRole.ADMIN` (the same check `requireDatasetPermission`'s `isSystemAdmin` already uses) so an unauthenticated/non-admin caller still sees only `{ status }`. **`cleanup.*` timestamps were descoped, documented in the route's own doc comment, not silently dropped**: the scheduled passes (Phases 6–7) run in a separate process and log only to stdout — nothing persists a queryable "last ran at" anywhere. Adding that would mean a new table purely for this, a real migration decision I'm not making unilaterally while extending an existing route. Flagged as a follow-up if wanted.
- [X] T052 [US6] Added `queueBacklog` via `webQueue.queue.getJobCounts()` (waiting + delayed), reusing `createWebQueue()` rather than a second client; returns `null` (not a thrown error) if the counts call itself fails, so a Redis hiccup degrades this one field rather than the whole response.
- [X] T053 [US6] Retrofitted `logJobEvent` at the central write sites not yet covered: `job.repository.ts#claimJob` (claimed+started, one atomic UPDATE used by every job type), `job-claim-lock.ts`'s `completeJob`/`failJob`/`cancelJob`, and `enqueue-job.ts`'s enqueue-success path plus all three of its Job-creation call sites (logged once, post-transaction-commit, and only on genuine fresh creation — not on an idempotent replay branch, and not inside a callback that can retry on a serialization conflict). **Scoped, not exhaustive**: other specialized creation flows (local-folder import preparation, media-processing job creation, AI task submission) are not swept — matches "basic operational visibility," not a mandate to touch every job-creating file in the codebase. Verified via real test runs (worker `test:queue` 41/41, web `test:job-queue` 32/32, both unchanged from before the retrofit) that nothing broke and no redaction-guard violation fired.
- [X] T054 [US6] `pnpm --filter @annotationplatform/web test:health` (script already present, added by your own parallel work): 3/3 pass. Quickstart §US6 satisfied by T048's real HTTP-server tests plus the log-output checks already demonstrated in Phases 3–7's own test runs.

**Checkpoint**: Platform health, backlog, and job-lifecycle logging are all observable through one extended, already-existing endpoint.

---

## Phase 9: User Story 7 - The Platform Survives Traffic Spikes Without Falling Over (Priority: P2)

**Goal**: Per-user Redis-backed rate limiting on the job-creating routes rejects excess requests with a consistent `429`, never touches internal worker operations, and never blocks legitimate in-budget usage.

**Independent Test**: Submit requests up to the configured limit (all succeed), one more (rejected with `429`), wait for window rollover (succeeds again); a second user is unaffected. Per `quickstart.md` §US7.

### Tests for User Story 7

- [X] T055 [P] [US7] Wrote `apps/web/tests/rate-limit/fixed-window.test.ts` — 5 tests, all passing against real Redis (key namespace `ratelimit:{randomUUID}:...` can't collide with anything else, safe to run against the shared instance directly). Covers: N-then-rejected; next-window recovery; per-user isolation; per-category isolation (bonus, beyond the original ask); and the fail-open behavior on a genuinely unreachable Redis (bounded-retry connection to a closed port) — confirmed both that the request is allowed *and* that the failure is logged (`RATE_LIMIT_CHECK_FAILED`), not silently swallowed.
- [X] T056 [P] [US7] Wrote `apps/web/tests/rate-limit/route-enforcement.test.ts` — 6 tests against a real rebuilt `next start` server, all passing, covering all 5 routes plus a "fresh user's first request is never blocked" check. **Found and fixed a real test-design bug before it could produce a false pass**: three of the five routes share `category: "import"`, and the rate-limit key is `ratelimit:{userId}:{category}:...` — scoped by category, not by route path. My first draft reused one user/cookie across all three "import" routes, which would have made each test's counter bleed into the next one's and produce misleading counts. Fixed by giving every single test its own freshly-created user. Also confirmed no regression: reran `test:job-queue` (32/32, unchanged), `test:local-folder-import` (19/20 — the same pre-existing, already-disclosed `ownership-matrix.test.ts` flake, not new), and `test:repository-import-request` (10/10) with the rate limiting live — nothing that previously passed now hits 429.

### Implementation for User Story 7

- [X] T057 [US7] Added `"RATE_LIMITED"` to the `ApiErrorCode` union; also added an optional `headers` param to `apiError()` (additive, backward-compatible) so the `Retry-After` header from `contracts/rate-limit-error.md` can be attached.
- [X] T058 [US7] Implemented `apps/web/src/lib/rate-limit/fixed-window.ts#checkRateLimit`. One shared, lazily-created connection per server process (not per-request like `createWebQueue()` — this runs on every job-creating request, and a fresh handshake each time would add needless latency to what's supposed to be a cheap check). **Explicit fail-open decision, documented in the code**: on a Redis failure, the check logs (`logRedisEvent`, never swallowed) and returns `allowed: true` rather than blocking the request — this counter is protective, not correctness-critical, and Redis is not authoritative for it (AGENTS.md), so failing closed would mean a Redis outage blocks all legitimate job creation too, a worse outcome than the abuse it guards against.
- [X] T059 [US7] Added the `ai-task` check to `POST /api/ai/tasks`, right after the auth check and before any body parsing — so it fires regardless of payload validity, and before `createAiTask` ever runs.
- [X] T060 [US7] Added the `import` check to all three routes, same placement (immediately after auth, before body/param parsing).
- [X] T061 [US7] Added the `export` check to `POST /api/export`, same placement.
- [X] T062 [US7] Confirmed structurally, not just asserted: `grep`'d `apps/worker/src` for any `fetch()` call to these route paths — zero matches. The worker consumes BullMQ deliveries and talks to Postgres/MinIO directly; it has no HTTP client pointed at `apps/web` anywhere in its source, so FR-039 holds by construction, not by a bypass flag that could be misused.
- [X] T063 [US7] `apps/web/package.json`'s `test:rate-limit` script already existed (added by your own parallel work) and matches the expected convention exactly. Ran both new test files together: 14/14 pass. Quickstart §US7 satisfied by T056's real end-to-end HTTP tests.

**Checkpoint**: All P1 + this P2 story complete — the platform now resists accidental or intentional job storms.

---

## Phase 10: User Story 8 - Large Lists Stay Fast and Usable as Data Grows (Priority: P2)

**Goal**: Every previously-unbounded list endpoint returns a capped page; the Properties Panel's Assets tab shows 10 per page; its Labels tab gains full name/color/color-code creation; "Add defaults" is explained in place.

**Independent Test**: Seed 20+ datasets/labels and 15+ assets in one dataset; confirm bounded pagination envelopes, Properties Panel 10-per-page behavior, label-creation parity with `/labels`, and unchanged asset/JobEvent pagination. Per `quickstart.md` §US8.

**Pre-implementation audit** (explicit instruction: audit and reuse the existing request-dedup/caching mechanisms — `dataset-labels-store.ts` and `dedupeInFlight` in `ai-task-api-client.ts` — before adding any new one). Traced every one of the 5 in-scope endpoints to its actual internal consumer(s) before writing a line of implementation:
  - `GET /api/datasets` — **zero internal consumers.** `(app)/datasets/page.tsx` is a Server Component running its own direct `db.dataset.findMany` with its own cursor pagination; it never calls this route.
  - `GET /api/datasets/[datasetId]/labels` — sole consumer is `dataset-labels-store.ts`'s `ensureLoaded` (module-singleton Zustand store, already built). Reused as-is, untouched.
  - `GET /api/source-connections` — **zero internal consumers.** `imports-screen.tsx` (renders `ImportForm`) is a Server Component running its own direct `db.sourceConnection.findMany`; it never calls this route either — same pattern as `/api/datasets`.
  - `GET /api/ai/models` — sole consumer is `listActiveAiModelsClient()` in `ai-task-api-client.ts`, already coalesced through its own `dedupeInFlight`. Reused as-is, untouched; it reads only `payload.data.models` and ignores unknown fields.
  - `GET /api/assets/[assetId]/annotations` — consumer is `getAssetAnnotations` → `image-engine.tsx`'s `applyAiResults(taskId)`, which re-fetches **every** annotation on an asset and filters client-side for one AI task's results. **This is the one endpoint where true pagination would be a correctness regression** (would silently drop AI-detect results past the first page) — deviated from `research.md`'s original assumption and applied a defensive `take` ceiling instead of a page/pageSize/total envelope; documented below.
  Conclusion: no new client-side store, cache, or fetch layer was needed or added anywhere in this phase. `dataset-labels-store.ts` and `dedupeInFlight` are the only two client-side data mechanisms touched, and both are unmodified.

### Tests for User Story 8

- [X] T064/T065 [P] [US8] Wrote `apps/web/tests/pagination/pagination-envelope.test.ts` (single file covering both, since T068/T069/T071's shared `parsePageRequest` helper made per-endpoint splitting artificial) — 7 tests, all passing against the real dev DB via a real rebuilt `next start` server: `GET /api/datasets` (`{ items, page, pageSize, total }` envelope, oversized `pageSize` clamped, beyond-last-page returns `items: []` + real `total` + `200`), `GET /api/datasets/[datasetId]/labels` (same envelope; confirmed an un-parameterized request — exactly how `dataset-labels-store.ts` calls it — still returns the dataset's full taxonomy), `GET /api/source-connections` and `GET /api/ai/models` (envelope alongside their existing `connections`/`models` keys; the AI-models total is asserted against a live `db.aiModel.count()` taken immediately before the request, never a fixed number, since that catalog is genuinely global/unscoped — the JobEvent-retention lesson from Phase 7 applied here), and `GET /api/assets/[assetId]/annotations` (proves `page`/`pageSize` query params have **no truncating effect** and the response shape has no `page`/`pageSize`/`total` fields at all — the defensive-cap-only endpoint). Every test uses its own freshly-created user/dataset; the two naturally actor-scoped endpoints (`datasets`, `source-connections`) can't cross-contaminate by construction, and the one genuinely global query (`aiModel.count`) is asserted relatively.
- [X] T066 [P] [US8] Wrote `apps/web/tests/pagination/frozen-endpoints.test.ts` — 2 tests, both passing. Asserts `GET /api/datasets/[datasetId]/assets` returns exactly `{ items, page: { limit, nextCursor, hasNextPage } }` and `GET /api/jobs/[jobId]/events` returns exactly `{ events, nextCursor }` (via `Object.keys(...).sort()` deep-equality on the response body, not spot-checks) — proving neither frozen endpoint picked up the new `page`/`pageSize`/`total` offset fields used elsewhere in this phase.
- [X] T067 [P] [US8] Confirmed no component-rendering test harness exists in this repo (no `@testing-library/react`/jsdom dependency in `apps/web/package.json`; only `vitest` for one non-DOM spec) — per AGENTS.md, did not add one. `AssetNavigator`'s own boundary JSX (`page <= 1`, `page * pageSize >= totalAssets`) is presentational only; what it actually consumes is `readWorkspacePage`'s `page`/`pageSize`/`total`, computed server-side. Wrote `apps/web/tests/workspace/asset-navigator-pagination.test.ts` against that real, database-backed source instead: 11 seeded assets at `WORKSPACE_ASSET_PAGE_SIZE = 10` — page 1 has 10 items with Next enabled, page 2 has the 11th item with Previous enabled/Next disabled, page 3 (beyond last) is empty with the real `total` still reported. Also **found and fixed two pre-existing tests whose assertions hardcoded the old `pageSize = 100`** (a real regression T072 would otherwise have silently caused): `tests/workspace/image-navigation.test.ts` (a selected-asset's page/previous-page/next-page numbers, recomputed for `pageSize = 10`) and `tests/workspace/image-workspace-http.test.ts` (a 250-asset fixture's per-page item counts, recomputed for `pageSize = 10`, with a bonus beyond-last-page assertion added since 250 is now exactly 25 full pages).

### Implementation for User Story 8

- [X] T068 [US8] Added `apps/web/src/lib/pagination.ts` (`parsePageRequest`, `paginateInMemory`) — a stateless request-parsing helper, not a cache or store; every route below still owns its own query. Wired into `GET /api/datasets`: `db.dataset.findMany({ ..., skip, take })` + `db.dataset.count(where)` in parallel, returns `{ items, page, pageSize, total }`. Default page size 20 (no internal caller to match, per the audit above).
- [X] T069 [US8] Same envelope on `GET /api/datasets/[datasetId]/labels`. Default page size is deliberately the server's own `PAGINATION_MAX_PAGE_SIZE` ceiling (100) so `dataset-labels-store.ts`'s existing unparameterized fetch keeps getting the dataset's full label taxonomy unless a dataset genuinely exceeds 100 labels — the store required zero code changes.
- [X] T070 [US8] No caller needed updating. The audit (above) found neither endpoint has an internal caller reading a bare array — `contracts/pagination-envelope.md`'s anticipated "breaking change" turned out not to exist in practice: both routes already returned `{ items: [...] }`, not a bare array, before this phase, so the added `page`/`pageSize`/`total` fields are purely additive for every caller, internal or external.
- [X] T071 [US8] `GET /api/source-connections` and `GET /api/ai/models`: their services (`listSourceConnections`, `listActiveAiModels`) each return a small, already-fully-fetched array with no Prisma-level pagination of their own, so `paginateInMemory` slices the array at the route rather than adding new skip/take plumbing to those services. `ai/models` keeps `dedupeInFlight`'s client untouched (verified it reads only `payload.data.models`). `GET /api/assets/[assetId]/annotations`: applied a defensive-only `take: 5_000` ceiling in `annotation-service.ts#readAssetAnnotations` — no envelope, no shape change, documented inline as a deliberate deviation from full pagination given the AI-detect-apply-results correctness risk found in the audit. Also added the previously-planned defensive `take: 1_000` ceiling to `image-workspace.ts`'s internal label query (no response-shape change; SSR-only, never fetched by the browser). Updated `specs/api/schemas/dataset.yaml`, `labels.yaml`, `ai.yaml`, and `import-repository.yaml`'s `SourceConnectionListResponse` (found during this task — an existing schema the earlier audit had missed) to document the new fields; `python3 scripts/validate-openapi.py` passes.
- [X] T072 [US8] The task's named file (`page.tsx`) doesn't compute `pageSize` itself — `workspace.page.pageSize` traces to one single source, `WORKSPACE_ASSET_PAGE_SIZE` in `apps/web/src/lib/workspace/workspace-assets.ts` (confirmed by grep: used nowhere else). Changed that one constant from `100` to `10`. No prop signature or component change needed anywhere, exactly as `research.md` decision 11 predicted.
- [X] T073 [US8] Replaced the name-only `<input>` + hardcoded `color: "#0EA5E9"` in both `image-properties-tabs.tsx` and `video-properties-tabs.tsx` (confirmed `placeholder-properties-tabs.tsx` has no such block) with a visual color-swatch `<input type="color">` + a synced hex `<input>` color-code field, both feeding the same `POST /api/datasets/[datasetId]/labels` call with the user-chosen (validated-hex, falling back to the old default) color instead of the hardcoded value — still calling `dataset-labels-store.ts`'s existing `addLabel` afterward, unchanged.
- [X] T074 [US8] Added an inline caption under the "Add defaults" button (image workspace only — confirmed the video Labels tab has no such button to caption) plus a `title` tooltip on the button itself, explaining it creates a standard starter label set and skips names that already exist. No change to `ensureDefaultImageLabelsAction`.
- [X] T075 [US8] `apps/web/package.json`'s `test:pagination` script already existed (pointing at `tests/pagination/**/*.test.ts`, added by your own parallel work) and matches the convention exactly. Full verification run: `test:pagination` 7/7, `frozen-endpoints` 2/2 (folded into the same script), `test:workspace` with `WORKSPACE_INTEGRATION_TESTS=1` 50/54 (3 skipped by design, 1 failing — `auth-pages.test.ts`'s stale session-count assertion, confirmed pre-existing and unrelated: the signup route's own comment says it deliberately never creates a session, the test still asserts one; nothing in this phase touches auth/session code), `test:dataset-metadata` 8/8, `test:rate-limit` 11/11, `test:health` 3/3, `test:openapi-contract` (0 run, all gated behind `OPENAPI_CONTRACT_TESTS=1`, none written for these 4 routes yet — none broken either), `test:ai` 32/36 against a live server (4 failures are AI-task queue/worker timing tests requiring a running `apps/worker` process, which wasn't started in this environment — unrelated to pagination), `tsc --noEmit` clean (only the one already-disclosed pre-existing unrelated failure in `legacy-image-content.test.ts`). `pnpm build` (`next build`) succeeds with the full route table intact.

**Checkpoint**: All P1 + P2 stories complete — lists stay fast, and the two named Properties Panel gaps are closed.

---

## Phase 11: User Story 9 - Local and Deployed Environments Start Reliably Together (Priority: P3)

**Goal**: The documented Docker Compose configuration brings up all five services with every new environment variable from this feature documented and defaulted.

**Independent Test**: Clean `docker compose up`, all five services healthy, one submitted job reaches a terminal state end-to-end. Per `quickstart.md` §US9.

### Implementation for User Story 9

- [X] T076 [US9] Added all 14 named vars plus one T003 also defines but the task list omitted (`MINIO_TEMP_UPLOAD_LIFECYCLE_DAYS`, read by `apps/worker/src/providers/minio.ts#ensureTempUploadLifecyclePolicy`) to `docker-compose.yaml`'s `web`/`worker` `environment:` blocks (worker-only vars on `worker`, web-only on `web`, matching `apps/worker/src/config.ts`/`apps/web/src/lib/config/production-hardening.ts` exactly) and to `.env.example`, using the same `${VAR:-default}` convention already in both files. `MINIO_ORPHAN_SCAN_DRY_RUN` is called out in an inline comment in every file it appears in: defaults to `true`, never flip it in a shared/committed file.
- [X] T077 [US9] `docker-compose.review.yaml` redeclares its own full `web`/`worker` environment blocks independently — added the same 15 vars there too. `docker-compose.preflight.yaml` does not redeclare the block; it's a Compose override file merged via `-f docker-compose.yaml -f docker-compose.preflight.yaml`, and Compose merges `environment:` maps key-by-key across `-f` files (confirmed with a read-only `docker compose ... config` render: the merged `web` env contains both the preflight override's own keys and every new key from the base file) — no edit needed there.
- [X] T078 [US9] Not from a clean checkout — both `docker-compose.yaml` (dev) and `docker-compose.review.yaml` (review) were already live, long-running stacks (46h/26h uptime) on this machine, not disposable. Validated both edited files read-only first (`docker compose -f <file> config -q`, exit 0 for both) before touching anything running. With the user's explicit go-ahead, recreated `web`+`worker` on the **dev** stack only (review stack never touched) to pick up the new env vars. `worker` recreated cleanly — confirmed running (`Fieldframe worker ready.`) with the new vars present and correct via `docker exec` (`JOB_EVENT_RETENTION_DAYS=30`, `MINIO_ORPHAN_SCAN_DRY_RUN=true`). **`web` did not come back up**: host port 3000 is held by an unrelated native `next-server` process already running on this machine (not started by this session, most likely the IDE's own live-preview/HMR server) — every `docker compose up` attempt failed to bind port 3000, and one such failed attempt left the `web` container fully detached from its Docker network (`docker inspect` showed empty `NetworkSettings.Networks`) until a further recreate attempt. Per the user's explicit instruction, stopped retrying rather than touching the native process or force-changing the container's published port unilaterally. Current state: `postgres`/`redis`/`minio`/`gitea`/`worker` all healthy/running on the dev stack; `web` is stopped (`Created`), to be brought up by the user (`docker compose -f docker-compose.yaml up -d web`) once port 3000 is free, or on request once that's confirmed.
- [X] T079 [US9] Port 3000 freed and `web` confirmed up by the user; `docker compose up -d web` alone still left the container fully network-detached (same symptom as T078 — a known quirk of this Compose version after a failed bind), so used `--force-recreate --no-deps web` instead, which came back correctly attached (`NetworkSettings.Networks` populated, `0.0.0.0:3000->3000/tcp` published) and answering `GET /api/health` → `200 {"status":"ready"}`. Ran one real, full end-to-end flow entirely through HTTP against the running container (own freshly-created user/dataset, cleaned up after): signup (`201`) → login (`200`, session cookie) → `POST /api/datasets` (`201`) → `POST /api/export` (`201`, Job `QUEUED`) → polled `GET /api/jobs/{jobId}` — reached `COMPLETED` (stage `FINISHED`, progress `100`) within one second → `GET /api/export/{jobId}` returned a real, working MinIO presigned download URL for the manifest artifact. This proves the full chain in one shot: `web` (Postgres write + BullMQ enqueue) → Redis → `worker` (dequeue, process, MinIO write, Postgres status update) → `web` (status read + presigned URL generation) — not just individual service liveness. Test fixture (Job/Dataset/User rows, exact IDs) deleted afterward via `docker exec ... psql`; the one small `exports/.../manifest-v1.json` object this produced was left in MinIO (same standing caution as [[minio-orphan-cleanup-deferred]] — a single harmless leftover, already covered by the existing disabled dry-run scanner, not worth an extra manual MinIO delete). Review stack (`docker-compose.review.yaml`) was never touched throughout T078/T079.

**Checkpoint**: All 9 user stories independently verified end-to-end.

---

## Phase 12: Polish & Cross-Cutting Concerns

**Purpose**: Horizontal-scaling verification spanning every mechanism above at once, plus final documentation/compliance sweep.

- [X] T080 [P] Wrote `apps/worker/tests/queue/horizontal-scaling.test.ts` — 4 tests, all passing against the real dev DB. (1) 8 jobs raced by two worker identities in one interleaved `Promise.all` (not one job at a time) — every job claimed by exactly one identity, confirmed via `lockedBy`/`status`. (2) Two identities' `recoverExpiredLeaseJobs` scans racing the same expired-lease job — `retried` sums to exactly 1 across both calls, `attempts` incremented exactly once (not twice). (3) Two identities' `withAdvisoryLock`-guarded passes with an artificial 200ms body delay — instrumented concurrency counter proves the bodies never overlap in wall-clock time (`maxConcurrentRunners === 1`), and the loser gets `ran: false` immediately (non-blocking `pg_try_advisory_lock`, never queues). (4) Bonus: two *different* lock keys (orphan-scan vs. temp-upload-cleanup) never block each other. Full `test:queue` rerun after adding this file: 70/70 (was 66/66; net +4, zero regressions).
- [X] T081 [P] Swept every `logJobEvent`/`logRedisEvent`/`logStorageEvent`/`logAiEvent`/`logMaintenanceEvent` call site across both apps (11 files) by hand, plus two targeted greps: one for any call spreading an object (`...`) into the fields argument (zero matches — every call site passes an explicit field literal, never a raw error/job object), one for any call site textually near `.input`/`.state`/`errorDetails`/`signedUrl`/`token`/`secret`/`accessKey`/`connectionString` (one incidental match, `enqueue-job.ts:34`'s `authorized.input.type` — a safe scalar sub-field read, not `Job.input` itself, confirmed by reading the surrounding code). Every call site is additionally type-constrained by `log.ts`'s closed per-category field types (`JobLogFields`/`RedisLogFields`/etc.), so a disallowed key couldn't type-check even before `assertSafe`'s runtime defense-in-depth would catch it. Zero fixes needed.
- [X] T082 [P] Ran the full suite. **Web** (13 `test:*` scripts, run individually since no aggregate script exists): every Phase 10/11-relevant suite green (`test:pagination` 7/7, `test:rate-limit` 11/11, `test:health` 3/3, `test:dataset-metadata` 8/8, `test:workspace` 36/36-non-skipped). Found and fixed a self-inflicted cross-file port collision (`direct-upload/upload-routes.test.ts` and `local-folder-import/http-fixtures.ts` both hardcode port 3115; a not-fully-reaped leftover server from the first polluted the second when run back-to-back in one shell) by killing the stray process — not a code fix, a test-run hygiene issue, confirmed by rerunning `test:local-folder-import` alone afterward (19/20, matching Phase 9's already-disclosed state exactly). Found four **pre-existing, unrelated** issues, none caused by 021, none touched by this session (confirmed via `git diff` showing zero changes to the affected files): `test:direct-upload` (7/8 fail — hardcodes `MINIO_PUBLIC_ENDPOINT: "http://minio:9000"`, unresolvable from a bare host shell, needs a Docker-network/CI context); `local-folder-import/ownership-matrix.test.ts` (already disclosed in Phase 9's report); `repository-preflight/source-import-contract.test.ts` (one assertion failure, reproducible in isolation, unrelated file never touched this session); `repository-import-worker/legacy-image-content.test.ts` (dead route reference, already disclosed via `tsc` earlier this session). **Worker**: `test:queue` 70/70, `test:source-access` 5/5, `test:repository-import` 13/13 (rest skipped, gated behind integration flags) — all green, zero failures. Deliberately did **not** re-enable `GARBAGE_COLLECTION_RUNTIME_TESTS=1` for this sweep (would re-run real MinIO/Postgres GC integration tests already verified extensively in Phase 6; redundant given no GC code changed since, and unnecessary live-infrastructure interaction given [[test-data-safety-discipline]]). **Typecheck**: both apps clean (one pre-existing, already-disclosed `legacy-image-content.test.ts` module error, same file as above). **Lint**: `apps/web` 0 errors/8 warnings (all in files never touched this session — one, `toolbar.tsx`, is the user's own pre-existing parallel-work diff); `apps/worker`'s lint script is `tsc --noEmit`, already covered.
- [X] T083 Walked every `quickstart.md` scenario against the actual test suite (not re-clicked by hand where an equivalent automated, passing test already exists at finer grain than the manual script): **US1** all 6 steps covered by `stale-job-detector.test.ts` (7 tests, including the explicit "running the scan again... is a no-op" and "two concurrent scan passes... leave exactly one winner" cases) plus T080's cross-mechanism version — PASS. **US2** covered by `recovery-eligibility.test.ts`, `stale-job-detector.test.ts`, and `bullmq-stalled.test.ts`'s real two-Worker stall/redelivery/claim-guard test — PASS. **US3** covered by `commit-route.test.ts` (including the TOCTOU race fix from Phase 5) and `import-timeout-scanner` coverage — PASS. **US4** all 7 steps covered by `minio-orphan-scanner.test.ts` (dry-run-vs-live, idempotent-rerun, and the concurrent-live-sweep test map directly onto steps 4-6), `deleted-asset-cleanup.test.ts`, `deleted-dataset-cleanup.test.ts`, `temp-upload-cleanup.test.ts` — PASS (live-mode deletion verified only against isolated fixture data per [[test-data-safety-discipline]]/[[minio-orphan-cleanup-deferred]], never against the real dev bucket's candidates). **US5** covered by `jobevent-retention.test.ts` — PASS. **US6** steps 1-2 covered by `health-observability.test.ts`; step 3's `cleanup.lastOrphanScanAt` **does not exist** in the shipped implementation — a documented Phase 8 deviation (no cross-process queryable "last ran" state without a new table, deliberately not added silently) — flagged for T084. **US7** covered by `fixed-window.test.ts` + `route-enforcement.test.ts` (6 routes, all 5 steps including the per-user/per-category isolation and the worker-side structural check) — PASS. **US8** steps 1 and 4 covered by `asset-navigator-pagination.test.ts`/`pagination-envelope.test.ts`/`frozen-endpoints.test.ts` — PASS; steps 2-3 (color-picker visual parity, "Add defaults" tooltip) have no browser/E2E harness in this repo (same gap T067 already documented) — verified by code review of the shipped JSX only, not a live interactive check. **US9** all 4 steps executed for real in T078/T079 against the actual dev Docker stack (not simulated) — PASS.
- [X] T084 One genuine deviation found and fixed: `quickstart.md` US6 step 3 referenced `cleanup.lastOrphanScanAt`, a field Phase 8 deliberately never implemented (documented limitation at the time: adding a queryable "last ran" timestamp means a new table, out of scope for "basic operational visibility"). Corrected that step in `quickstart.md` to match the shipped `checks`/`jobs`-only health body. No other `spec.md`/`plan.md`/`data-model.md`/`research.md` cross-reference needed correction — every other implementation decision (the annotations defensive-cap deviation from Phase 10, the TOCTOU fix from Phase 5, the PreparedImportItem reference-check narrowing from Phase 6) was already reconciled into its own phase's contract/task notes as it was found, not deferred to this final sweep.

**Feature checkpoint**: All 9 user stories implemented and independently verified (8 via automated integration tests against real Postgres/Redis/MinIO, 1 — US9 — via a real Docker Compose stack end-to-end run). Two items remain explicitly outside this feature's authority to resolve unilaterally, both already disclosed to and acknowledged by the user: live deletion of the 532 pre-existing MinIO orphan candidates found in Phase 6 ([[minio-orphan-cleanup-deferred]]), and a live browser/E2E check of US8's two UI-only steps (no test framework for it exists in this repo, and none was added, per AGENTS.md's "do not add npm packages without explicit permission").

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies.
- **Foundational (Phase 2)**: Depends on Setup — **blocks every user story phase**.
- **US1–US4 (Phases 3–6, all P1)**: Depend only on Foundational; independent of each other, but US2's BullMQ stall handling (T019) and US1's stale detector (T010) are complementary (`research.md` decision 3) — implement US1 first since US2's tests (T017) exercise US1's scheduling pattern as a reference.
- **US5–US8 (Phases 7–10, all P2)**: Depend only on Foundational; US6's health surface (Phase 8) reads counts that are more meaningful once US1/US4 exist, so implement after Phase 3–6 even though there's no hard code dependency.
- **US9 (Phase 11, P3)**: Benefits from every env var Phases 2–10 introduce existing already — do last among the stories.
- **Polish (Phase 12)**: Depends on all desired stories being complete; T080 specifically needs US1 (T010/T012) and US4 (T034) done.

### Within Each User Story

Tests → implementation → scheduling/logging wiring → suite run, in the order listed within each phase.

### Parallel Opportunities

- All Foundational tasks (T003–T007) run in parallel — five different files.
- Within each user story, all `[P]`-marked test tasks run in parallel (different files); most `[P]` implementation tasks likewise (different modules), except where a task explicitly says "depends on T0xx."
- US1–US4 (Phases 3–6) can be staffed in parallel once Phase 2 is done, since each touches a disjoint set of new worker modules; the only shared touch point is `readiness.ts`'s scheduling block (T012/T013/T034/T040/T045 all add an interval there) — coordinate those five edits to avoid merge conflicts, or land them sequentially.
- US5–US8 (Phases 7–10) are similarly parallelizable once Phase 2 is done.

---

## Parallel Example: User Story 1

```bash
# Tests together:
Task: "Write stale-RUNNING detection/recovery/dead-letter tests in apps/worker/tests/queue/stale-job-detector.test.ts"
Task: "Write multi-worker-identity concurrency test in apps/worker/tests/queue/stale-job-detector-concurrency.test.ts"

# Then implementation (T010 blocks T011/T013/T014; T012 is independent of T010/T011):
Task: "Wire recovery-scanner.ts into readiness.ts (T012)"          # parallel with T010
Task: "Implement stale-job-detector.ts's lease-based branch (T010)" # parallel with T012
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Phase 1 (Setup) → Phase 2 (Foundational) → Phase 3 (US1).
2. **STOP and VALIDATE**: run `quickstart.md` §US1 against a real worker-crash simulation.
3. This alone converts "a crashed worker strands the job forever" into "the platform self-heals," the single highest-value guarantee in this feature.

### Incremental Delivery

1. Setup + Foundational → foundation ready.
2. US1 → validate → deploy (MVP: crash recovery).
3. US2 → validate → deploy (outage resilience joins crash recovery).
4. US3 → validate → deploy (import timeout, mostly test-coverage — low risk, fast to land).
5. US4 → validate → deploy (storage GC — highest blast radius, land after the job-lifecycle guarantees above are proven).
6. US5–US8 → validate → deploy, in any order (all P2, independent).
7. US9 → validate → deploy (deployment documentation/verification).
8. Phase 12 → cross-cutting concurrency proof + final sweep.

### Suggested MVP Scope

**User Story 1 alone** (Phases 1–3: T001–T015). It is independently deployable, independently testable via `quickstart.md` §US1, and delivers the feature's foundational reliability guarantee without depending on any other story.
